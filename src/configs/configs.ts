// this should probably be in a .env file but for the sake of simplicity going to put it here
export const configs = {
  PORT: 3010,
  DOMAIN: 'localhost',
  APP_URL: 'http://localhost:3010',
  JWT_SECRET: 'should probably change this',
  SALT_ROUNDS: 12,
};
